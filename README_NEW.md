# AfricaJustice (reorganized)

Project reorganized to a modular structure under `src/` with directories for `components`, `pages`, `services`, `utils`, `hooks`, and `context`.

This README was added as README_NEW.md to avoid overwriting the existing README. Replace the original as needed.
